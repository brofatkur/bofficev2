'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { KeyRound } from 'lucide-react';

export default function LoginPage(){const router=useRouter();const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);setError('');const raw=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch('/api/auth/sign-in',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(raw)});const out=await res.json();setLoading(false);if(!res.ok){setError(out.error||'Tidak dapat masuk.');return}const staff=['super_admin','branch_admin','finance','sales'].includes(out.data?.role);router.push(staff?'/':'/portal');router.refresh()};return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><img src="/logo.webp" alt="BOffice" className="mx-auto h-10 w-auto"/><div className="mt-7 text-center"><KeyRound className="mx-auto h-8 w-8 text-blue-600"/><h1 className="mt-3 text-xl font-extrabold">Masuk BOffice</h1><p className="text-sm text-slate-500">Admin pusat, tim cabang, customer, reseller, dan mitra properti.</p></div><label className="mt-6 block text-xs font-bold">Email<input name="email" type="email" required className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-3"/></label><label className="mt-4 block text-xs font-bold">Password<input name="password" type="password" required className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-3"/></label>{error&&<p className="mt-4 rounded-xl bg-rose-50 p-3 text-xs text-rose-700">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-blue-600 py-3 text-sm font-bold text-white disabled:opacity-50">{loading?'Memeriksa akun...':'Masuk'}</button><p className="mt-4 text-center text-[11px] text-slate-400">Hubungi admin BOffice jika belum menerima undangan akun.</p></form></div>}
