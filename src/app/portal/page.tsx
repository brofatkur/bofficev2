'use client';
import { useEffect, useMemo, useState } from 'react';
import { Building2, FileText, LogOut, TrendingUp, Users } from 'lucide-react';
const money=(n:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n||0);
export default function PortalPage(){const [data,setData]=useState<any>(null);const [error,setError]=useState('');useEffect(()=>{fetch('/api/portal').then(async r=>({ok:r.ok,out:await r.json()})).then(({ok,out})=>{if(!ok){setError(out.error);if(out.error==='Sesi tidak aktif.')location.href='/login'}else setData(out.data)})},[]);const metrics=useMemo(()=>{if(!data)return[];if(data.role.role==='customer')return[{label:'Total invoice',value:data.invoices.length,icon:FileText},{label:'Sisa tagihan',value:money(data.invoices.reduce((s:number,x:any)=>s+Number(x.remaining_amount),0)),icon:TrendingUp},{label:'Dokumen',value:data.documents.length,icon:FileText}];if(data.role.role==='reseller')return[{label:'Klien',value:new Set(data.invoices.map((x:any)=>x.customer_id)).size,icon:Users},{label:'Omzet diterima',value:money(data.invoices.reduce((s:number,x:any)=>s+Number(x.total_paid),0)),icon:TrendingUp},{label:'Komisi diperoleh',value:money(data.commissions.reduce((s:number,x:any)=>s+Number(x.earned_amount),0)),icon:FileText}];const income=data.transactions.filter((x:any)=>x.direction==='income').reduce((s:number,x:any)=>s+Number(x.amount),0),expense=data.transactions.filter((x:any)=>x.direction==='expense').reduce((s:number,x:any)=>s+Number(x.amount),0);return[{label:'Properti',value:data.branches.length,icon:Building2},{label:'Penjualan diterima',value:money(income),icon:TrendingUp},{label:'Pengeluaran',value:money(expense),icon:FileText}]},[data]);if(!data)return <div className="fixed inset-0 z-[100] grid place-items-center bg-slate-950 text-sm text-white">{error||'Memuat portal...'}</div>;const title=data.customer?.company_name||data.partner?.name||data.user.email;return <div className="fixed inset-0 z-[90] overflow-y-auto bg-slate-100"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><img src="/logo.webp" alt="BOffice" className="h-8 w-auto"/><button onClick={async()=>{await fetch('/api/auth/sign-out',{method:'POST'});location.href='/login'}} className="flex items-center gap-2 text-xs font-bold text-slate-500"><LogOut className="h-4 w-4"/>Keluar</button></div></header><main className="mx-auto max-w-6xl space-y-6 p-5 lg:p-8"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-blue-600">Portal {String(data.role.role).replace('_',' ')}</p><h1 className="mt-1 text-2xl font-extrabold">{title}</h1><p className="text-sm text-slate-500">Informasi yang ditampilkan khusus untuk akun Anda.</p></div><div className="grid gap-4 md:grid-cols-3">{metrics.map((m:any)=><div key={m.label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><m.icon className="h-5 w-5 text-blue-600"/><p className="mt-3 text-xs font-bold text-slate-400">{m.label}</p><p className="mt-1 text-xl font-extrabold">{m.value}</p></div>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-extrabold">Aktivitas terbaru</h2><div className="mt-4 space-y-2">{data.role.role==='customer'?data.invoices.map((x:any)=><Row key={x.id} title={x.invoice_number} note={`Jatuh tempo ${x.due_date}`} value={money(x.remaining_amount)}/>):data.role.role==='reseller'?data.invoices.map((x:any)=><Row key={x.id} title={x.invoice_number} note={`Terbayar ${money(x.total_paid)}`} value={money(x.reseller_commission)}/>):data.transactions.slice(0,30).map((x:any)=><Row key={x.id} title={x.description} note={x.transaction_date} value={`${x.direction==='income'?'+':'−'} ${money(x.amount)}`}/>)}</div></div></main></div>}
function Row({title,note,value}:{title:string,note:string,value:string}){return <div className="flex items-center justify-between rounded-xl border border-slate-100 p-3 text-xs"><div><strong>{title}</strong><p className="text-slate-400">{note}</p></div><strong>{value}</strong></div>}
