'use client';
import { useState } from 'react';
import { CheckCircle2, KeyRound } from 'lucide-react';

export default function InvitePage({params}:{params:{token:string}}){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [message,setMessage]=useState('');const [done,setDone]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(password!==confirm){setMessage('Konfirmasi password tidak sama.');return}const res=await fetch('/api/invitations/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:params.token,password})});const out=await res.json();setMessage(out.success?'Akun berhasil diaktifkan. Silakan masuk ke Portal BOffice.':out.error);setDone(Boolean(out.success))};return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950 p-4"><div className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><img src="/logo.webp" alt="BOffice" className="mx-auto h-10 w-auto"/>{done?<div className="mt-8 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-emerald-500"/><h1 className="mt-4 text-xl font-extrabold">Akun aktif</h1><p className="mt-2 text-sm text-slate-500">{message}</p><a href="/login" className="mt-6 block rounded-xl bg-blue-600 py-3 text-sm font-bold text-white">Masuk ke portal</a></div>:<form onSubmit={submit} className="mt-8 space-y-4"><div className="text-center"><KeyRound className="mx-auto h-8 w-8 text-blue-600"/><h1 className="mt-3 text-xl font-extrabold">Aktifkan akun</h1><p className="text-sm text-slate-500">Buat password untuk Portal BOffice.</p></div><label className="block text-xs font-bold">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-3"/></label><label className="block text-xs font-bold">Ulangi password<input value={confirm} onChange={e=>setConfirm(e.target.value)} type="password" minLength={8} required className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-3"/></label>{message&&<p className="rounded-xl bg-rose-50 p-3 text-xs text-rose-700">{message}</p>}<button className="w-full rounded-xl bg-blue-600 py-3 text-sm font-bold text-white">Aktifkan akun</button></form>}</div></div>}
